{deps, [
    conf,
    pdm_logger
]}.

{profiles, [
    {test, [
        {deps, [
            testutils
        ]}
    ]}
]}.

{xref_ignores, [
    {core_link_parser, return_error, 2} % this function is automatically generated, so ignore warning
]}.