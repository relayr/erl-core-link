{deps, [
    conf,
    pdm_logger
]}.

{profiles, [
    {test, [
        {deps, [
            {testutils, {git, "https://github.com/relayr/pdm-erl-testutils.git", {branch, "master"}}}
        ]}
    ]}
]}.

{cover_enabled, true}.
